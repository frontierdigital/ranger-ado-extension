{
  "name": "ranger-ado-extension",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "run-p build:*",
    "build:install-ranger-task": "npm --prefix ./tasks/InstallRangerCLI install && npm --prefix ./tasks/InstallRangerCLI run build",
    "clean": "run-p clean:*",
    "clean:install-ranger-task": "npm --prefix ./tasks/InstallRangerCLI run clean",
    "release": "run-p release:*",
    "release:install-ranger-task": "npm --prefix ./tasks/InstallRangerCLI run release",
    "test": "run-p test:*",
    "test:install-ranger-task": "npm --prefix ./tasks/InstallRangerCLI test"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:frontierdigital/ranger-ado-extension.git"
  },
  "author": "Frontier Digital Ltd",
  "license": "GPL-3.0",
  "bugs": {
    "url": "https://github.com/frontierdigital/ranger-ado-extension/issues"
  },
  "homepage": "https://github.com/frontierdigital/ranger-ado-extension#readme",
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "tfx-cli": "^0.15.0"
  }
}